Main 
    html
        body
            div.header Welcome to the NAV DEMO Page!
            
    style.
        body {
            font-size: 1.3rem;
        }
        .header {
            width: 100vw;
            height: 10vh;
            text-align: center;
            font-size: 20px;
            background-color: rgb(170, 255, 170);
        }
    script.
        //var connection = new WebSocket('ws://localhost:8080', ['soap', 'xmpp']);

        /*connection.onmessage = function(e) {
            window.location = "http://localhost:3000/nav/" + e.data
        }*/