Error 
    include Mixins/socket.pug
    include Mixins/stylePackage.pug
    // params: {}
    html
        body
            div.header Error, the command you have stated is not within our database. Please try again
            
    style.
        body {
            font-size: 1.3rem;
        }
        .header {
            width: 100vw;
            height: 10vh;
            text-align: center;
            font-size: 20px;
        }
    +socketScript()
    +stylePackage()